# get tag_id with api_url ,cookies, headers and tag_name

# add tag to song
# get song_id, tag_id, api_url, cookies and headers
# return status code and message

# remove tag from song
# get song_id, tag_id, cookies and headers
# return status code and message